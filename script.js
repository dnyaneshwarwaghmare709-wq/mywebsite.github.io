function downloadCV() {
  alert("Add your CV file in the project folder and link it here!");
}

document.getElementById("contactForm")?.addEventListener("submit", function(e) {
  e.preventDefault();
  alert("Message sent! (Demo only)");
});
